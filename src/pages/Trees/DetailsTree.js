import React from 'react';
import useTitle from '../../hocks/useTitle';

const DetailsTree = () => {
    useTitle("tree/details");
    return (
        <div>
            <p>Tree Details</p>
        </div>
    );
};

export default DetailsTree;